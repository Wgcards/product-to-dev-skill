# App Asset Routing

App 端示例、模板、脚手架片段和检查清单后续统一放在本目录下。

当前状态：placeholder。

- App lane 规范仍在整理中，当前不提供可复制 App 模板或示例。
- 不从 `assets/react-tailwind-template/` 生成 App 项目。
- 不从 `assets/ssr/` 复用 Next.js SSR/BFF 模板。
- 后续 App 示例建议放 `assets/app/examples/`。
- 后续 App 模板建议放 `assets/app/templates/`。

在 App 规范补齐前，命中 App lane 的需求只做范围识别、handoff 和缺口记录；如用户明确要求实现，应先确认目标技术栈与既有项目约定。
